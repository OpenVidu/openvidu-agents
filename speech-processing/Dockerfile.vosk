# syntax=docker/dockerfile:1.6
# =============================================================================
# Dockerfile for Vosk STT with default language models.
# =============================================================================

ARG BASE_IMAGE=openvidu/agent-speech-processing-vosk-base:main
FROM ${BASE_IMAGE}

COPY --chown=appuser:appuser vosk-models ./vosk-models
